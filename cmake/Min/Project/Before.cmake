# Chain to max-sdk-base's Before.cmake; min externals need the same pre-project setup
# (MSVC /MT runtime flags, macOS deployment target, output directory configuration)
include("${MAXSDK_BASE_DIR}/cmake/Max/Project/Before.cmake")
